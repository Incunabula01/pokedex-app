This project is a React Typescript app based on data from the [Poke API](https://pokeapi.co/). 

### Getting Started

Clone the repo and install dependencies:
```bash
npm install
```

Then run the dev enviroment:
```bash
npm start
```


